export module Main;

import EasyGui;

export int main(int argc, char* argv[]) {
    // This is the entry point of the program.
    // You can add your code here to execute when the program starts.
    return 0; // Return 0 to indicate successful execution.
}